spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/med
    username: oussama
    password: oa2002
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    default-encoding: UTF-8
  jackson:
    serialization:
      write-date-keys-as-timestamps: false
    time-zone: UTC


application:
  security:
    jwt:
      expiration: 86400000 # a day
      refresh-key:
        expiration: 604800000 # 7 days
  mail:
    url: http://localhost:8080/auth/verify?token=

